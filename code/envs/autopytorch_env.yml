name: research_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - pyyaml
    - requests
    - tqdm
    - numpy
    - pandas
    - wandb
    - autoPyTorch
    - scikit-learn